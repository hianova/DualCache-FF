| 測試用例 ID | 描述 | 分類 | 狀態 | 預期結果 |
|------------|------|------|------|----------|
| **CT-01** | 空快取呼叫 `count()` | 邊界條件 | ✅ 設計完成 | 返回 `0` |
| **CT-02** | 空快取呼叫 `avg()` | 邊界條件 | ✅ 設計完成 | 返回 `None`（或等價空值） |
| **CT-03** | 插入單一鍵值對後呼叫 `count()` | 基礎功能 | ✅ 設計完成 | 返回 `1` |
| **CT-04** | 插入單一鍵值對後呼叫 `avg()` | 基礎功能 | ✅ 設計完成 | 返回該值本身 |
| **CT-05** | 插入多個不同鍵，數量小於容量，呼叫 `count()` | 正確性 | ✅ 設計完成 | 返回插入的鍵數量 |
| **CT-06** | 插入多個不同鍵，數量小於容量，呼叫 `avg()` | 正確性 | ✅ 設計完成 | 返回手動計算之平均值 |
| **CT-07** | 重複插入相同鍵（更新值），驗證 `count()` 不變 | 冪等性 | ✅ 設計完成 | `count()` 保持不變 |
| **CT-08** | 重複插入相同鍵（更新值），驗證 `avg()` 反映最新值 | 冪等性 | ✅ 設計完成 | 平均值根據最後寫入的值重新計算 |
| **CT-09** | 插入直至容量滿 `(count = N)`，驗證 `count()` | 容量邊界 | ✅ 設計完成 | 返回 `N` |
| **CT-10** | 容量滿時插入新鍵，觸發淘汰，驗證 `count()` 保持 `N` | 淘汰正確性 | ✅ 設計完成 | 仍返回 `N` |
| **CT-11** | 容量滿時插入新鍵，觸發淘汰，驗證 `avg()` 更新為淘汰後剩餘項的平均值 | 淘汰正確性 | ✅ 設計完成 | 平均值與手動計算相符 |
| **CT-12** | 連續插入與刪除（若支援顯式刪除）後驗證計數與平均 | 進階操作 | ⏳ 待設計 | 依 API 定義 |
| **CT-13** | 並發環境下多線程同時寫入，`count()` 最終一致性 | 並發安全性 | ⏳ 待設計 | 最終值等於成功寫入的鍵數（不超過容量） |
| **CT-14** | 並發讀寫混合下 `avg()` 的即時一致性（依賴快取一致性模型） | 並發安全性 | ⏳ 待設計 | 觀察值應為某時刻的快照平均 |
| **CT-15** | 值包含極大整數（如 `u64::MAX`），計算平均值時不溢出 | 數值邊界 | ✅ 設計完成 | 正確計算且無 panic |
| **CT-16** | 值包含負數（若支援有號數），平均值符號正確 | 數值邊界 | ✅ 設計完成 | 正確處理正負值混合 |
| **CT-17** | 值為浮點數（若支援），驗證精度與捨入誤差 | 浮點精度 | ⏳ 待設計 | 誤差在合理範圍內 |
| **CT-18** | 淘汰策略影響平均值：使用 LRU / LFU 等不同策略時，淘汰後平均值計算一致 | 策略正確性 | ⏳ 待設計 | 僅與剩餘集合相關，與策略無關 |